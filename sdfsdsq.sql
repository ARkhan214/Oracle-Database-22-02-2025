select job_id
From hr.employees
WHERE substr (job id,4)='REP';

select first_name,commission_pct
from employees
WHERE commission_pct is null;

SELECT First_name,Last_name,commissio_pct
from employees
;

select job_id
from employees
where sub




Select last_name
from employees;
where substr(last_name,2,2)='an';

select last_name
from employees
where substr(last_name, 2,2)='an';

select last_name
from employees
where substr(last_name -1)='n';

